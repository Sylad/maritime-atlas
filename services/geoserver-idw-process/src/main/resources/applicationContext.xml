<?xml version="1.0" encoding="UTF-8"?>
<!--
  Spring application context — discovered automatiquement par GeoServer
  au boot (Spring ContextLoaderListener scanne tous les
  classpath*:applicationContext.xml).

  Declare notre IDWProcessFactory comme un bean Spring. Combiné avec
  SPI dans META-INF/services/, ça permet à GeoServer de :
   - charger la classe Factory au démarrage
   - exposer les processes annotés @DescribeProcess via WPS GetCapabilities
   - les rendre invocables dans les SLD <Transformation>
-->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         https://www.springframework.org/schema/beans/spring-beans.xsd">

  <bean id="idwProcessFactory"
        class="fr.sladoire.maritime.idw.IDWProcessFactory"/>

</beans>
