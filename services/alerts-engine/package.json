{
  "name": "maritime-alerts-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Subscribe ais.positions + lightning.strike + raster.ready, apply rules, emit alerts.maritime + persist to PG",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.5",
    "@types/node": "^22.5.5",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.0"
  }
}
