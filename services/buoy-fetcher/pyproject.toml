[project]
name = "buoy-fetcher"
version = "0.1.0"
description = "Fetch EMODnet Physics wave platform referential (Europe-wide) → PostGIS"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
    "psycopg[binary]>=3.2.0",
    "apscheduler>=3.10.4",
]
